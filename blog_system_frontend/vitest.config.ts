import { defineConfig } from 'vitest/config';
import react from '@vitejs/plugin-react-swc';
import path from 'path';

export default defineConfig({
  plugins: [react()],
  test: {
    globals: true,
    environment: 'jsdom',
    setupFiles: ['./src/test/setup.ts'],
    include: ['src/**/*.{test,spec}.{js,mjs,cjs,ts,mts,cts,jsx,tsx}'],
    coverage: {
      provider: 'v8',
      reporter: ['text', 'json', 'html'],
      include: ['src/**/*.{ts,tsx}'],
      exclude: [
        'src/components/**',        // сгенерированные компоненты из библиотеки shadcn/ui — не содержат кастомной бизнес-логики и не требуют покрытия тестами
        'src/hooks/use-toast.ts',   // хук уведомлений, предоставленный shadcn/ui как готовое решение — не является частью собственной логики приложения
        'src/hooks/use-mobile.ts',  // хук определения мобильного устройства из шаблона shadcn — переиспользуемый утилитарный код без специфики проекта
        'src/lib/utils.ts',         // утилитарные функции (например, clsx-обёртка), сгенерированные или скопированные из shadcn/ui — не содержат уникальной логики проекта и не тестируются отдельно
        'src/mocks/**',             // моки используются только в тестах и не участвуют в реальной работе приложения
        'src/pages/**',             // страницы часто содержат только компонентную обвязку без бизнес-логики, нецелесообразно измерять их покрытие
        'src/shared/config/env.ts', // конфигурационные переменные, не содержат никакой логики
        'src/test/**/*',            // вспомогательные файлы для тестов (например, моки, утилиты) не являются частью основной логики
        'App.tsx',                  // корневой компонент приложения — обычно только рендер маршрутов, не содержит тестируемой логики
        'main.tsx',                 // точка входа в приложение, содержит только инициализацию (рендер, провайдеры); не подлежит покрытию
        'vite-env.d.ts',            // файл типизации для Vite, не содержит исполняемого кода
        '*.test.{ts,tsx}',          // исключаем сами тестовые файлы — они не должны учитываться в покрытии
      ],
    },
  },
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
});
